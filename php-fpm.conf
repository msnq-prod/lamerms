; Minimal PHP-FPM overrides for AdamRMS MVP
[www]
; Allow PHP to read environment variables used for bootstrap configuration
clear_env = no
; Keep long-running exports and seeding jobs responsive before timing out
request_terminate_timeout = 600s
