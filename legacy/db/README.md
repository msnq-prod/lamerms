# Legacy migrations

This directory archives the original AdamRMS migration set. They are retained for
reference but are no longer executed by the default Phinx configuration. The
active schema is defined by `db/migrations/20240101000000_core_schema.php`.
